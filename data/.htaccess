# Prevent directory listing
Options -Indexes

# (OPTIONAL) Block script execution (e.g., PHP) in /data for security
<FilesMatch "\.(php|pl|py|cgi|asp|aspx|jsp|sh)$">
  Order Allow,Deny
  Deny from all
</FilesMatch>
